/** Gameplay distances kept in one place so client and server stay aligned. */

/** Radius around a laptop where the [E] interact prompt appears. */
export const LAPTOP_INTERACT_RADIUS = 1.6;

/** Radius around a quest item where the [E] prompt appears (also walk-in completion). */
export const QUEST_INTERACT_RADIUS = 2.0;

/** Distance at which the catch anticipation cue arms, ~300ms of teacher travel outside the 1.2m server catch radius. */
export const CATCH_ANTICIPATION_DISTANCE = 2.6;

/** Networking */
export const SEND_HZ = 15;
