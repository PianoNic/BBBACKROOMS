[pytest]
testpaths = tests
# Services are async top to bottom; `auto` mode means tests don't each need an
# @pytest.mark.asyncio decorator.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q
